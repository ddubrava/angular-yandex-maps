import { YaPanoramaDirective } from './ya-panorama.directive';

describe('YaPanoramaDirective', () => {
  it('should create an instance', () => {
    const directive = new YaPanoramaDirective();
    expect(directive).toBeTruthy();
  });
});
