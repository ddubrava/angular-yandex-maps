import { YaControlDirective } from './ya-control.directive';

describe('Directive: YaControl', () => {
  it('should create an instance', () => {
    const directive = new YaControlDirective();
    expect(directive).toBeTruthy();
  });
});
